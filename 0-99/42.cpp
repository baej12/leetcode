#include <iostream>
#include <vector>

using namespace std;

int solve(vector<int>);

int solve(vector<int> height) {
  for (const auto i : height) {
    if (i > 0) {
      
    }
  }
}

int main () {
  return 0;
}